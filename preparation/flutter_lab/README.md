# AppCon AI Lab

Pre-event Android integration exercise, not the final entry. See ../../docs/FLUTTER_AI.md for configuration and ../../docs/VALIDATION.md for verification boundaries.

Use `bash ../../scripts/setup-lab.sh` from this directory. Firebase project configuration and a supported Android device are required for real inference. Do not publish private credentials or App Check debug tokens.
